# GET /dashboard/events 엔드포인트 흐름

> **목적**: Dashboard 페이지의 Events 표에 표시할 이벤트 데이터를 조회
>
> **최종 업데이트**: 2026-01-05 (I-43 - txn_price_trend 테이블 분리 설계)

---

## 1. 엔드포인트 개요

| 항목 | 값 |
|------|-----|
| **경로** | `GET /dashboard/events` |
| **라우터 파일** | `backend/src/routers/dashboard.py` |
| **프론트엔드 파일** | `frontend/src/pages/DashboardPage.jsx`, `frontend/src/components/dashboard/EventsTable.jsx` |

### 쿼리 파라미터

| 파라미터 | 타입 | 기본값 | 설명 |
|---------|------|--------|------|
| `page` | int | 1 | 페이지 번호 (1부터 시작) |
| `pageSize` | int | 50 | 페이지당 행 수 (최대 1000) |
| `sortBy` | string | null | 정렬 컬럼명 |
| `sortOrder` | string | null | 정렬 방향 (asc/desc) |
| `ticker` | string | null | 티커 필터 (부분 일치) |
| `sector` | string | null | 섹터 필터 (부분 일치) |
| `industry` | string | null | 업종 필터 (부분 일치) |
| `source` | string | null | 소스 필터 (부분 일치) |
| `condition` | string | null | 조건 필터 (부분 일치) |

**사용법 예시**:
```bash
# 1. 기본 조회 (페이지 1, 50행)
GET /dashboard/events

# 2. 페이지 2, 100행씩
GET /dashboard/events?page=2&pageSize=100

# 3. 티커 필터 + 정렬
GET /dashboard/events?ticker=AAPL&sortBy=event_date&sortOrder=desc

# 4. 복합 필터
GET /dashboard/events?sector=Technology&industry=Software&page=1&pageSize=200
```

---

## 2. 응답 구조

### EventsResponse
```json
{
  "data": [
    {
      "id": "uuid-string",
      "ticker": "AAPL",
      "event_date": "2024-12-22",
      "sector": "Technology",
      "industry": "Consumer Electronics",
      "source": "fmp-price-target",
      "source_id": "analyst-123",
      "position_quantitative": "long",
      "disparity_quantitative": 0.15,
      "position_qualitative": "short",
      "disparity_qualitative": -0.05,
      "condition": "upgrade,target_increase",
      "wts": 7,
      "d_neg14": -0.023,
      "d_neg13": -0.015,
      ...
      "d_pos14": 0.089
    }
  ],
  "total": 146696,
  "page": 1,
  "pageSize": 100
}
```

---

## 3. 호출 흐름도 (현재 구현)

```
[Client - DashboardPage.jsx]
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ useEffect(() => { fetchEvents() }, [page, pageSize, ...])      │
│   ├─► Build query parameters (page, pageSize, sortBy, filters) │
│   └─► fetch(`/dashboard/events?${params}`)                     │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ routers/dashboard.py:157-437                                    │
│ @router.get("/events", response_model=EventsResponse)          │
│ async def get_events(...)                                       │
│   ├─► Phase 1: Build WHERE clause (filters)                    │
│   ├─► Phase 2: Build ORDER BY clause (sortBy, sortOrder)       │
│   ├─► Phase 3: Calculate offset = (page - 1) * pageSize        │
│   ├─► Phase 4: Execute COUNT query (total rows)                │
│   ├─► Phase 5: Execute SELECT query (paginated data)           │
│   └─► Phase 6: Parse price_trend JSONB (현재 병목)             │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ Phase 6 상세: price_trend JSONB 파싱 (병목 구간)                │
│                                                                 │
│ for row in rows:  # 100행 반복                                  │
│   raw_price_trend = row["price_trend"]                          │
│   price_trend = {}                                              │
│                                                                 │
│   # 타입별 파싱 (str/dict/list)                                 │
│   if isinstance(raw_price_trend, str):                          │
│     parsed = json.loads(raw_price_trend)                        │
│     if isinstance(parsed, dict):                                │
│       price_trend = parsed                                      │
│   elif isinstance(raw_price_trend, dict):                       │
│     price_trend = raw_price_trend                               │
│   elif isinstance(raw_price_trend, list):                       │
│     price_trend = {}                                            │
│                                                                 │
│   # 28개 dayOffset 추출 (D-14 to D14, excluding D0)             │
│   for offset in range(-14, 15):  # 28번 반복                    │
│     if offset == 0: continue                                    │
│     key = f"D{offset:+d}"                                       │
│     value = price_trend.get(key) or price_trend.get(alt_key)   │
│     day_values[offset] = float(value) if value else None        │
│                                                                 │
│   # WTS 계산 (position_multiplier 적용)                         │
│   max_return = None                                             │
│   for offset, value in day_values.items():                      │
│     adjusted_return = value * position_multiplier               │
│     if max_return is None or adjusted_return > max_return:      │
│       max_return = adjusted_return                              │
│       wts = offset                                              │
│                                                                 │
│ # EventRow 객체 생성 및 반환                                     │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
[Client - EventsTable.jsx]
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ Render DataTable with 29 columns (D-14 to D14 including WTS)   │
└─────────────────────────────────────────────────────────────────┘
```

**현재 성능 문제**:
- 100행 기준: 2,800번의 딕셔너리 조회 (100행 × 28 dayOffsets)
- Python JSONB 파싱: ~1.5초
- 전체 응답 시간: 2-5초

---

## 4. 호출 흐름도 (I-43 개선 후)

```
[Client - DashboardPage.jsx]
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ useEffect(() => { fetchEvents() }, [page, pageSize, ...])      │
│   ├─► Build query parameters (page, pageSize, sortBy, filters) │
│   └─► fetch(`/dashboard/events?${params}`)                     │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ routers/dashboard.py:157-??? (I-43 수정)                       │
│ @router.get("/events", response_model=EventsResponse)          │
│ async def get_events(...)                                       │
│   ├─► Phase 1: Build WHERE clause (filters)                    │
│   ├─► Phase 2: Build ORDER BY clause (sortBy, sortOrder)       │
│   ├─► Phase 3: Calculate offset = (page - 1) * pageSize        │
│   ├─► Phase 4: Execute COUNT query (total rows)                │
│   ├─► Phase 5: Execute JOIN query ✨ NEW                        │
│   └─► Phase 6: Direct JSONB read (파싱 불필요) ✨ NEW           │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ Phase 5: JOIN Query (I-43 개선)                                │
│                                                                 │
│ SELECT                                                          │
│     e.*,                                                        │
│     p.d_neg14, p.d_neg13, ..., p.d_0, ..., p.d_pos14,          │
│     p.wts_long, p.wts_short                                     │
│ FROM txn_events e                                               │
│ LEFT JOIN txn_price_trend p                                     │
│     ON e.ticker = p.ticker                                      │
│     AND e.event_date = p.event_date                             │
│ WHERE {filters}                                                 │
│ ORDER BY {sortBy} {sortOrder}                                   │
│ LIMIT {pageSize} OFFSET {offset}                                │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ Phase 6: Direct JSONB Read (파싱 불필요) ✨                      │
│                                                                 │
│ for row in rows:  # 100행 반복                                  │
│   # JSONB 직접 읽기 (파싱 없음!)                                │
│   d_neg14_data = row['d_neg14']  # 이미 dict 타입               │
│                                                                 │
│   if d_neg14_data:                                              │
│     d_neg14_performance = d_neg14_data['performance']['close']  │
│     d_neg14_close = d_neg14_data['price_trend']['close']       │
│                                                                 │
│   # WTS는 컬럼에서 직접 조회                                     │
│   if row['position_quantitative'] == 'long':                    │
│     wts = row['wts_long']                                       │
│   else:                                                         │
│     wts = row['wts_short']                                      │
│                                                                 │
│ # EventRow 객체 생성 및 반환                                     │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
[Client - EventsTable.jsx]
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ Render DataTable with 29 columns (D-14 to D14 including WTS)   │
└─────────────────────────────────────────────────────────────────┘
```

**개선 후 성능**:
- 100행 기준: JSONB 파싱 0회 (PostgreSQL이 dict로 반환)
- Python 처리: ~50ms (97% 감소)
- 전체 응답 시간: 0.2-0.4초 (85-92% 개선)

---

## 5. 성능 비교

### 현재 구현 (I-43 이전)

| 단계 | 작업 | 시간 |
|------|------|------|
| 1 | WHERE 절 구성 | ~5ms |
| 2 | ORDER BY 절 구성 | ~5ms |
| 3 | Offset 계산 | ~1ms |
| 4 | COUNT(*) 쿼리 | ~100ms |
| 5 | SELECT 쿼리 (JSONB 포함) | ~400ms |
| **6** | **JSONB 파싱 (병목)** | **~1500ms** |
| 7 | EventRow 객체 생성 | ~50ms |
| 8 | 네트워크 전송 | ~200ms |
| **합계** | - | **~2.3초** |

### I-43 개선 후

| 단계 | 작업 | 시간 | 변화 |
|------|------|------|------|
| 1 | WHERE 절 구성 | ~5ms | - |
| 2 | ORDER BY 절 구성 | ~5ms | - |
| 3 | Offset 계산 | ~1ms | - |
| 4 | COUNT(*) 쿼리 | ~100ms | - |
| **5** | **JOIN 쿼리** | **~100ms** | **✨ 400ms → 100ms** |
| **6** | **Direct JSONB 읽기** | **~50ms** | **✨ 1500ms → 50ms** |
| 7 | EventRow 객체 생성 | ~30ms | ✅ 감소 |
| 8 | 네트워크 전송 | ~100ms | ✅ 감소 (페이로드 동일) |
| **합계** | - | **~0.4초** | **🎉 83% 개선** |

---

## 6. 구현 체크리스트 (I-43)

- [ ] **Step 1**: txn_price_trend 테이블 생성 (DDL)
  - [ ] 29개 dayOffset 컬럼 (d_neg14 ~ d_pos14)
  - [ ] wts_long, wts_short 컬럼
  - [ ] PRIMARY KEY (ticker, event_date)
  - [ ] 인덱스 생성

- [ ] **Step 2**: 데이터 마이그레이션
  - [ ] 기존 txn_events.price_trend → txn_price_trend 변환 스크립트
  - [ ] WTS 계산 및 저장

- [ ] **Step 3**: dashboard.py 수정
  - [ ] JOIN 쿼리로 변경
  - [ ] JSONB 파싱 로직 제거
  - [ ] Direct JSONB 읽기로 교체
  - [ ] WTS 컬럼 직접 조회

- [ ] **Step 4**: 성능 테스트
  - [ ] 100행 조회 시간 측정
  - [ ] 1000행 조회 시간 측정
  - [ ] 필터/정렬 성능 확인

- [ ] **Step 5**: 프론트엔드 확인
  - [ ] API 응답 형식 호환성 확인
  - [ ] 데이터 표시 정상 작동 확인

- [ ] **Step 6**: 클린업 (선택)
  - [ ] txn_events.price_trend 컬럼 제거

---

## 7. 관련 이슈

- **I-20**: POST /backfillEventsTable 성능 개선 (티커 배치 처리) - ✅ 완료
- **I-24**: price trends 처리 성능 최적화 (거래일 캐싱) - ✅ 완료
- **I-43**: Dashboard Events 표 로딩 성능 개선 (본 이슈) - 🔄 진행중

---

*최종 업데이트: 2026-01-05 KST (I-43 설계 완료)*
*참조: history/I-43_FLOW.md, history/1_CHECKLIST.md#I-43*
